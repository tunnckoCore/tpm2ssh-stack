# Candidate Finding

## Summary
- ID: F-001
- Title: Capability recommendation logic overstates PRF/Seed support for coupled one-sided use sets
- Component: capability recommendation / mode selection
- Path(s): src/backend/recommend.rs; src/model/core.rs; src/ops/enforcement.rs; cross-ref: src/ops.rs, src/model/capability.rs
- Bug class: policy/model mismatch
- Root Cause Key: backend/recommend.rs:supports_mode:missing-coupled-use-contracts:false-capability-acceptance

## Hypothesis
- Suspected issue: `supports_mode(...)` in the recommendation layer accepts `verify`-only, `decrypt`-only, and `ssh`-only requests for PRF/Seed, while the canonical coupled-use contract later rejects them.
- Why it may matter: inspect/auto-mode/model consumers can be told a capability surface is truthful even though setup fails closed when asked to materialize it.
- Trust boundary involved: capability report / model truthfulness boundary used before identity creation.

## Evidence so far
- Static evidence: `src/backend/recommend.rs` checks allowed mode surface but omits the coupled-use rules enforced by `UseCase::validate_for_mode(...)` in `src/model/core.rs`.
- Dynamic evidence: repository tests pass for both contradictory behaviors.
- Oracle strength: strong

## Reproduction status
- Repro available? [yes]
- Minimal trigger idea: ask recommendation layer whether PRF supports `Verify` or `Ssh`; compare with setup-time `resolve_identity(...)` behavior for the same request.
- Preconditions: none beyond compiled test environment.

## Root cause
- Suspected root cause: recommendation logic duplicated part of the use-policy model instead of reusing the canonical coupled-use validator.
- Confidence: high
- Competing explanations: none that fit the existing tests as well as direct contract drift.

## Next step to validate or reject
- Best next oracle: correlate targeted cargo test runs against the exact contradictory test cases.
- Owner: review agent
