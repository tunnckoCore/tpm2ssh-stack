[package]
name = "tpmctl-core"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Reusable core contracts for TPM-backed tpmctl operations"

[features]
default = []
tpm = ["dep:tss-esapi"]

[dependencies]
tss-esapi = { version = "7.6.0", optional = true }
